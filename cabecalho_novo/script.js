function enter(elemento, corEntrada) { //O elemento, é o elemento no html que queremos, e a cor entrada, 
    elemento.style.color = corEntrada  //é a cor que queremos, que será colocada no parâmetro
}
function out(elemento, corSaida) { //Mesma coisa que o enter
    elemento.style.color = corSaida
}
